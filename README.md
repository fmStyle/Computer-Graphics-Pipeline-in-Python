Use arrow keys to control the movement.

Turn the variable "wireframe" to True to see the wireframe instead.

It uses pygame to initialize a window and a grid of rectangles simulating a screen.

Uses Blinn-Phong illumination model.

Beware the performance might not be great.
